<section class="bg-pink-50 text-black px-4 py-12 text-center relative z-[100] -mt-[220px] sm:-mt-[515px]">
    <h2 class="text-lg font-bold sm:text-2xl">Join the unbannable short-form video platform!</h2>
    <p class="mt-2 text-sm sm:text-base">All your favorite features, without the risk of losing your online presence.</p>
    <a href="#" class="inline-block mt-4">
        <img src="/assets/downloadOnTheAppStore.svg" alt="App Store" class="w-32 h-auto" />
    </a>
    <div class="mt-[10px] overflow-x-auto gap-2 flex items-center h-[180px] sm:h-[366px]">
        {[...Array(10)].map((_, i) => (
            <img
                src={`/assets/carousel${i + 1}.png`}
                class="
                    w-auto
                    flex-none
                    transition-all duration-300
                    h-full
                    sm:h-[244px]
                    sm:hover:h-full
                "
            />
        ))}
    </div>
</section>
